server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tmdghks2515/made-for-maid.config-files
          default-label: main
          username: anan
          password: ${GITHUB_MAVEN_TOKEN_KEY}
